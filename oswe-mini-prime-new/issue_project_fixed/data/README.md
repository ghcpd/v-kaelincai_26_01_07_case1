# Data Directory

This folder contains the sample datasets used by the normalizer tests and examples:

- `sample_high_correlation.json` - 10-sample dataset with high inter-dimension correlation
- `sample_high_correlation.csv` - 20-sample dataset with high inter-dimension correlation
- `sample_low_correlation.json` - 10-sample dataset with low inter-dimension correlation
