# Sample Data Files

This directory contains the same example datasets as the original project. Files and schema are unchanged — only the normalizer implementation in `src/` was fixed to be deterministic.

Files included:
- `sample_high_correlation.json` (10 samples)
- `sample_high_correlation.csv` (20 samples)
- `sample_low_correlation.json` (10 samples)

See the top-level README for usage and verification steps.
