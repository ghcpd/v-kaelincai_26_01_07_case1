# Data Directory

This directory contains sample data files for testing the data normalization functionality.

- `sample_high_correlation.json`: JSON data with high correlation between features
- `sample_high_correlation.csv`: CSV version of the high correlation data
- `sample_low_correlation.json`: JSON data with low correlation between features